[package]
name = "bondry-http-server"
description = "Secure shared local HTTP runtime for Bondry protocol adapters"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true

[features]
default = ["mcp", "rest"]
mcp = ["dep:bondry-mcp-proto"]
rest = ["dep:bondry-rest-proto"]
unix-socket = ["dep:nix"]
tls = ["dep:rustls", "dep:tokio-rustls", "dep:zeroize"]

[dependencies]
base64.workspace = true
bondry-core = { version = "0.3.0", path = "../../contract/bondry-core" }
bondry-mcp-proto = { version = "0.3.0", path = "../../proto/bondry-mcp-proto", optional = true }
bondry-rest-proto = { version = "0.3.0", path = "../../proto/bondry-rest-proto", optional = true }
bytes.workspace = true
http.workspace = true
http-body-util.workspace = true
hyper = { workspace = true, features = ["http1", "server"] }
hyper-util.workspace = true
nix = { workspace = true, optional = true }
rustls = { workspace = true, optional = true }
serde_json.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["macros", "net", "rt", "sync", "time"] }
tokio-rustls = { workspace = true, optional = true }
zeroize = { workspace = true, optional = true }

[dev-dependencies]
base64.workspace = true
tempfile.workspace = true

[lints]
workspace = true
