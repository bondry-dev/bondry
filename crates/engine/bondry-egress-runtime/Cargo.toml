[package]
name = "bondry-egress-runtime"
description = "Bounded current-thread runtime for Bondry egress"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
bondry-delivery-store = { version = "0.1.2", path = "../../contract/bondry-delivery-store" }
bondry-egress = { version = "0.1.2", path = "../../egress/bondry-egress" }
bondry-secrets = { version = "0.1.2", path = "../../contract/bondry-secrets" }
bondry-transport = { version = "0.1.2", path = "../../contract/bondry-transport" }
bytes.workspace = true
thiserror.workspace = true
tokio.workspace = true

[lints]
workspace = true
