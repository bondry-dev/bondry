[package]
name = "bondry-store-sqlcipher"
description = "Optional encrypted SQLCipher persistence for Bondry authentication and audit events"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
base64.workspace = true
bondry-auth = { path = "../bondry-auth" }
bondry-core = { path = "../bondry-core" }
getrandom.workspace = true
thiserror.workspace = true
zeroize.workspace = true

[target.'cfg(target_vendor = "apple")'.dependencies]
rusqlite = { workspace = true, features = ["bundled-sqlcipher", "cache"] }

[target.'cfg(not(target_vendor = "apple"))'.dependencies]
rusqlite = { workspace = true, features = ["bundled-sqlcipher-vendored-openssl", "cache"] }

[dev-dependencies]
futures = "0.3"
serde_json.workspace = true
tempfile.workspace = true

[lints]
workspace = true
